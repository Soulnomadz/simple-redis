
use simple_redis::*;
fn main() {
    // let n = 15;
    // let mut moves = Vec::new();
    // hanoi(n, 'A', 'B', 'C', &mut moves);

    // println!("Total steps: {}", &moves.len());
    // println!("Total memory used: {}", std::mem::size_of_val(&moves));

    // println!("Unit size: {}", std::mem::size_of::<(&str, &str)>());
}
